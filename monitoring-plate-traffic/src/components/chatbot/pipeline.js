// User gõ / bấm câu hỏi
//         ↓
// matchQuery() → xác định loại query
//         ↓
// extractTime() + extractPlate() → lấy tham số
//         ↓
// Gọi API backend → query DB
//         ↓
// Format kết quả → hiện trong chat